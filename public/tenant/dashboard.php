<?php require (is_file(__DIR__.'/../../app/bootstrap.php') ? __DIR__.'/../../app/bootstrap.php' : '/var/www/appsbilling-commercial-platform/app/bootstrap.php'); $u=require_tenant_user(); $tenant=q1('SELECT * FROM tenants WHERE id=?',[(int)$u['tenant_id']]); $db=tenant_db_path($tenant); $pdo=new PDO('sqlite:'.$db); $pdo->setAttribute(PDO::ATTR_DEFAULT_FETCH_MODE,PDO::FETCH_ASSOC); $stats=['packages'=>$pdo->query('SELECT COUNT(*) FROM packages')->fetchColumn(),'customers'=>$pdo->query('SELECT COUNT(*) FROM customers')->fetchColumn(),'invoices'=>$pdo->query('SELECT COUNT(*) FROM invoices')->fetchColumn(),'payments'=>$pdo->query('SELECT COUNT(*) FROM payments')->fetchColumn()]; render_header('Dashboard Mitra'); ?><div class="dash"><aside class="side"><div class="brand"><span class="brand-mark">AB</span><span><?=e($tenant['company_name'])?></span></div><a class="active" href="<?=app_url('tenant/dashboard.php')?>">Dashboard</a><a href="#">Pelanggan</a><a href="#">Tagihan</a><a href="#">Pembayaran</a><a href="#">Kwitansi</a><a href="<?=app_url('tenant/settings.php')?>">Logo & Branding</a><a href="<?=app_url('tenant/logout.php')?>">Keluar</a><p class="footer-note">Tenant DB: <?=e(basename(dirname($db)))?></p></aside><main class="main"><div class="topline"><div><h1>Dashboard Billing Mitra</h1><p>Workspace billing milik <?=e($tenant['company_name'])?>. No Akun: <strong><?=e($tenant['account_no'] ?? '-')?></strong>. Database sudah terpisah dari tenant lain.</p><?php if(!empty($u['is_master_admin'])):?><p><span class="badge pending">Mode Admin Pusat</span></p><?php endif;?></div><span class="badge active">active</span></div><div class="grid"><div class="card"><h3>Pelanggan</h3><p><b><?=$stats['customers']?></b> data</p></div><div class="card"><h3>Paket</h3><p><b><?=$stats['packages']?></b> paket</p></div><div class="card"><h3>Tagihan</h3><p><b><?=$stats['invoices']?></b> invoice</p></div></div><div class="card" style="margin-top:18px"><h3>Flow aktif</h3><p>Registrasi → approval admin → provision DB tenant → login tenant sudah berjalan. Modul billing detail berikutnya akan mengambil feature parity AppsBilling V3 tanpa menyentuh DSG `/v3` dan NMS.</p><div class="alert alert-warn">Tahap MVP awal: tenant shell dan DB isolated sudah aktif. Logo aplikasi/kwitansi sudah bisa diatur dari menu Logo & Branding.</div></div></main></div><?php render_footer(); ?>
